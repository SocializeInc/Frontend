import MainBlock from '../../UI/MainBlock';
import Card from '../../UI/Card'; 
import './User.css';

const User = () => {
  return(
    <MainBlock>
      <Card>
      </Card>
    </MainBlock>
  );
};

export default User;